#import os

# Directory to read files from
#directory_path = 'C:\\Users\\newti\\Desktop\\FTPfolder'

# Path for the log file
#log_file_path = 'file_log.txt'

# Change the current directory to the specified path
#os.chdir(directory_path)

# Get the list of files in the specified directory, excluding 'client.log' and 'file_log.txt'
#files_in_directory = [f for f in os.listdir('.') if os.path.isfile(f) and f not in ['client.log', 'file_log.txt']]

# Write the file names to the log file
#with open(log_file_path, 'w') as log_file:
    #for file in files_in_directory:
        #log_file.write(f"{file}\n")

#print(f"Log file created: {log_file_path}")

